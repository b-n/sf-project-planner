const initialState = {
  isLoggedIn = false
}

const LoginReducer = (state = initialState, action) => {
  switch (action.type) {
    case '':
      return state
    default:
      return state
  }
}

export default LoginReducer
